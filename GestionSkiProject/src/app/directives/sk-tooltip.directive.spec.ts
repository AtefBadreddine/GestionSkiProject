import { SkTooltipDirective } from './sk-tooltip.directive';

describe('SkTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new SkTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
